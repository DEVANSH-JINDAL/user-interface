spring:
  application:
    name: user-interface
#  datasource:
#    url: jdbc:mysql://localhost:3306/user_interface
#    username: admin
#    password: admin
#  jpa:
#    hibernate:
#      ddl-auto: update
#      show-sql: true
#spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true